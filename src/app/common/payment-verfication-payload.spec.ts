import { PaymentVerficationPayload } from './payment-verfication-payload';

describe('PaymentVerficationPayload', () => {
  it('should create an instance', () => {
    expect(new PaymentVerficationPayload()).toBeTruthy();
  });
});
